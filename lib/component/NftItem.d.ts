import { DomNode, Store } from "skydapp-browser";
export default class NftItem extends DomNode {
    id: number;
    store: Store;
    private checkbox;
    constructor(name: string, id: number);
    deselect(): void;
}
//# sourceMappingURL=NftItem.d.ts.map